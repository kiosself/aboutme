<%= form_for(aboutme) do |f| %>
  <% if aboutme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aboutme.errors.count, "error") %> prohibited this aboutme from being saved:</h2>

      <ul>
      <% aboutme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
